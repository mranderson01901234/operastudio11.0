# MCP Connectors

Place Modular Connector Protocol (MCP) integrations in this directory. Each connector should export a `streamChat` function that adheres to the shared interface defined in `lib/chat/session.ts`.
